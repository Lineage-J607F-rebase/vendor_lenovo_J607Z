on property:init.svc.mediadrm=running
    mkdir /data/vendor/mediadrm 0770 media mediadrm
    start vendor.move_data_sh

service vendor.move_data_sh /system/bin/move_widevine_data.sh
    class late_start
    user media
    group media mediadrm system
    disabled
    oneshot

service vendor.drm-widevine-hal-1-3 /vendor/bin/hw/android.hardware.drm@1.3-service.widevine
    interface android.hardware.drm@1.0::ICryptoFactory widevine
    interface android.hardware.drm@1.0::IDrmFactory widevine
    interface android.hardware.drm@1.1::ICryptoFactory widevine
    interface android.hardware.drm@1.1::IDrmFactory widevine
    interface android.hardware.drm@1.2::ICryptoFactory widevine
    interface android.hardware.drm@1.2::IDrmFactory widevine
    interface android.hardware.drm@1.3::ICryptoFactory widevine
    interface android.hardware.drm@1.3::IDrmFactory widevine
    interface android.hidl.base@1.0::IBase widevine
    class hal
    user media
    group media mediadrm drmrpc system
    ioprio rt 4
    writepid /dev/cpuset/foreground/tasks
    disabled

# widevine TA unavailable on this build: the A13 blob links CBS_init which
# the A16 VNDK libcrypto no longer exports, so the service dies at exec and
# init restarts it in a 5s crash loop (sets sys.init.updatable_crashing).
# Keep the VINTF declaration (drm services require it) but never start the
# service: framework falls back to clearkey, identical to pre-boot state.
# (prev: deferred start at boot_completed - still looped afterwards)
